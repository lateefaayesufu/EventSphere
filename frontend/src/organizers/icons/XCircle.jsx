// src/components/icons/XCircle.jsx

import React from "react";
import { XCircle } from "lucide-react";

const XCircleIcon = (props) => {
  return <XCircle {...props} />;
};

export default XCircleIcon;
