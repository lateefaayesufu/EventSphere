// src/components/icons/Calendar.jsx

import React from "react";
import { Calendar } from "lucide-react";

const CalendarIcon = (props) => {
  return <Calendar {...props} />;
};

export default CalendarIcon;
